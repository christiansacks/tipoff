services:
  cyberready:
    volumes:
      - .:/app                    # live-mount source — changes reload without rebuild
      - cyberready_data:/data
    command: uvicorn main:app --host 0.0.0.0 --port 8080 --reload
